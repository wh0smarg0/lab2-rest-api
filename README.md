# Lab2 REST API

Простий Flask REST API для обліку користувачів, категорій і витрат.  
Підходить для запуску локально або в хмарі (Render).  

---

## 📂 Структура проекту

```

lab2-rest-api/
├─ app.py             # Основний файл Flask з усіма ендпоінтами
├─ requirements.txt   # Python-залежності
├─ README.md
└─ postman_collection.json  # Колекція запитів для тестування

````

---

## 🖥 Локальний запуск

1. Створити та активувати віртуальне середовище:

```bash
python -m venv .venv
# Linux / macOS
source .venv/bin/activate
# Windows
.venv\Scripts\activate
````

2. Встановити залежності:

```bash
pip install -r requirements.txt
```

3. Запустити сервер:

```bash
python app.py
```

4. Перевірка роботи:

* [http://127.0.0.1:5000/users](http://127.0.0.1:5000/users) — список користувачів
* [http://127.0.0.1:5000/category](http://127.0.0.1:5000/category) — список категорій
* [http://127.0.0.1:5000/record](http://127.0.0.1:5000/record) — список записів (фільтри: `user_id`, `category_id`)

---

## ☁️ Деплой на Render

1. Створити новий **Web Service** на Render.
2. Підключити репозиторій з проектом.
3. Вказати **Python** як середовище.
4. Render автоматично задасть змінну середовища `PORT`.
5. У `app.py` сервер слухає `0.0.0.0` і підхоплює порт:

```python
import os

port = int(os.environ.get("PORT", 5000))
app.run(host="0.0.0.0", port=port, debug=True)
```

6. Деплой має стартувати без помилок, і API буде доступний на URL Render.

---

## 📦 Ендпоінти API

### Користувачі (Users)

| Метод  | Шлях         | Опис                                                   |
| ------ | ------------ | ------------------------------------------------------ |
| POST   | `/user`      | Створити користувача                                   |
| GET    | `/users`     | Отримати список всіх користувачів                      |
| GET    | `/user/<id>` | Отримати користувача за ID                             |
| DELETE | `/user/<id>` | Видалити користувача за ID (повертає ім’я користувача) |

---

### Категорії (Categories)

| Метод  | Шлях             | Опис                                                |
| ------ | ---------------- | --------------------------------------------------- |
| POST   | `/category`      | Створити категорію                                  |
| GET    | `/category`      | Отримати всі категорії                              |
| DELETE | `/category/<id>` | Видалити категорію за ID (повертає назву категорії) |

---

### Записи витрат (Records)

| Метод  | Шлях                            | Опис                                                       |
| ------ | ------------------------------- | ---------------------------------------------------------- |
| POST   | `/record`                       | Створити запис витрат                                      |
| GET    | `/record?user_id=&category_id=` | Отримати записи з фільтром по користувачу та/або категорії |
| GET    | `/record/<id>`                  | Отримати конкретний запис                                  |
| DELETE | `/record/<id>`                  | Видалити запис за ID (повертає деталі запису)              |

---

## 🧪 Тестування через Postman

1. Імпортуй `postman_collection.json`.
2. Використовуй готові запити для перевірки всіх ендпоінтів.
3. **Спочатку створюй користувачів і категорії**, потім записи.

---

## ℹ️ Примітки

* API зберігає дані **тільки в пам’яті** — після перезапуску всі дані зникають.
* Для локального запуску використовуйте `http://127.0.0.1:5000`.
* Для деплою на Render використовуйте URL Render, порт вказувати не потрібно.

---

# Lab3 REST API
* **Виконала:** Сахно Маргарита
* **Група:** 35
* **Варіант:** 2 (Розрахунок: 35 % 3 = 2)
* **Опис:** Реалізовано API для обліку фінансів з використанням PostgreSQL, 
SQLAlchemy та Marshmallow. Додано підтримку приватних та загальних категорій.

---

# 🛠 Технології та Функціонал
* База даних: PostgreSQL у Docker-контейнері. 
* ORM: SQLAlchemy для управління таблицями user, category, record. 
* Міграції: Flask-Migrate для оновлення структури БД. 
* Варіант №2: Реалізовано "Користувацькі категорії". Категорії бувають двох типів:
  1. Загальні — створюються без user_id, видимі всім. 
  2. Приватні — створюються з прив'язкою до user_id, видимі лише власнику.

---

# 🚀 Локальний запуск

1. Запустити базу даних (Docker):

```bash
docker-compose up -d db
```

2. Встановити залежності:

```bash
pip install -r requirements.txt
```

3. Виконати міграції:

```bash
flask db upgrade
```

4. Запустити сервер:
```bash
flask run
```

---

# 📦 Основні Ендпоінти

| Метод | Шлях                  | Опис                                                                   |
|-------|-----------------------|------------------------------------------------------------------------|
| POST  | `/user`               | Створення користувача                                                  |
| POST  | `/category`           | Створення категорії (загальної або приватної)                          |
| GET   | `/category?user_id=X` | Отримання доступних категорій для користувача (загальні + його власні) |
| POST  | `/record`             | Створення витрати                                                      |
| GET   | `/record?user_id=X`   | Фільтрація витрат користувача                                          |


# 🧪 Тестування

У репозиторії додано два файли для Postman:

    postman_collection.json — колекція запитів.

    postman_environment.json — налаштування змінної base_url

---

## Lab4 REST API

### Мета роботи:
* Реалізація реєстрації та входу користувачів з хешуванням паролів.
* Захист ендпоїнтів API за допомогою **JWT (JSON Web Tokens)**.
* Інтеграція бази даних **PostgreSQL** (у Docker) для постійного зберігання даних.
* Реалізація **Варіанту №2**: Загальні та приватні категорії.

---

### Основні функції:
1. **Безпека**: 
   * Паролі ніколи не зберігаються у відкритому вигляді. Використано бібліотеку `passlib` з алгоритмом `pbkdf2_sha256`.
   * Доступ до категорій та записів захищено декоратором `@jwt_required()`.
2. **Логіка бази даних (Варіант №2)**:
   * **Загальні категорії**: Категорії, де `user_id` має значення `null`, доступні для всіх користувачів.
   * **Приватні категорії**: Категорії, прив'язані до конкретного `user_id`, бачить лише їхній власник.
3. **Автоматизація тестування**:
   * Налаштовано середовище (Environment) та скрипти **Tests** у Postman для автоматичного захоплення `access_token` після логіну та його підстановки в наступні запити.

---

### Технологічний стек:
* **Flask-JWT-Extended**: Для керування токенами.
* **Passlib**: Для безпечного хешування паролів.
* **PostgreSQL**: Основна база даних.
* **Docker Compose**: Для оркестрації контейнера бази даних.

---

### Як запустити:
1. Запустіть базу даних: 
```bash
`docker-compose up -d`
```

2. Встановіть залежності:
```bash
`pip install -r requirements.txt`
```

3. Запустіть додаток:
```bash
`flask run`
```
4. Використовуйте **Postman Flow**: 
```bash
Register -> Login -> Create/Get Data
```
